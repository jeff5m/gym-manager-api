spring:
  application:
    name: GymManager
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
    initialization-mode: never
  jpa:
    hibernate:
      ddl-auto: create
